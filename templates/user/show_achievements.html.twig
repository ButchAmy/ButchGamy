{% extends 'base.html.twig' %}

{% block title %}ButchGamy - {{ user.username }}'s Profile{% endblock %}

{% block body %}
	<div class="container my-3">
		{{ include("user/_show.html.twig") }}
		<ul class="nav nav-tabs my-3">
			<li class="nav-item">
				<a class="nav-link" href="{{ path('app_user_show_games', {'id': user.id}) }}">Games</a>
			</li>
			<li class="nav-item">
				<a class="nav-link active" aria-current="page" href="{{ path('app_user_show_achievements', {'id': user.id}) }}">Achievements</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('app_user_show_stats', {'id': user.id}) }}">Stats</a>
			</li>
		</ul>
		{{ include('achievement/_grid.html.twig') }}
	</div>
{% endblock %}
